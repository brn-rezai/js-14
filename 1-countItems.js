//Count the number of <li> in the countItems.html file and show the results in console.

const fruitCount = document.querySelectorAll( "li" );
let count = fruitCount.length;
console.log( `we have ${ count } fruit` );
